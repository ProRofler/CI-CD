#pragma once

#include <array>
#include <string>

namespace cmds_list {
std::array<std::string, 11> commands_list = {"cmd1", "cmd2", "cmd3", "cmd4",
                                             "cmd5", "cmd6", "cmd7", "cmd8",
                                             "cmd9", "help", "exit"};
}
