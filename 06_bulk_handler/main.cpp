#include <iostream>

#include "bulk_handler.hpp"

int main([[maybe_unused]] int argc, [[maybe_unused]] char *argv[]) {
    // BULK commmands handler

    bulk_handler bulk;
    bulk.start();

    return 0;
}